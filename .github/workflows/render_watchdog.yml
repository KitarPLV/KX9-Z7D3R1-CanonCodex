
name: Render Deployment Watchdog

on:
  workflow_run:
    workflows: ["Sync and Snapshot"]
    types:
      - completed

jobs:
  monitor-render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: python telemetry/deploy_watchdog.py
